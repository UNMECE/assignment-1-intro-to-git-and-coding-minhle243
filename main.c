#include <stdio.h>
#include <math.h>                                                                                       double sum(double arr[], int size){
        double total=0;                                                                                         for(int i=0;i<size;i++){                                                                                        total=total+arr[i];}                                                                            return total;                                                                                   }
double average(double arr[], int size){
        return sum(arr,size)/size;
}
double std_devitation(double arr[], int size){                                                                  double avg = average(arr,size);                                                                         double total =0;                                                                                        for(int i=0;i<size;i++){                                                                                        total=total+(arr[i]-avg)*(arr[i]-avg);                                                          }                                                                                                       return sqrt(total/size);                                                                        }
int main(){                                                                                                     int size;                                                                                               double arr[100];                                                                                        printf("Enter the number of value: ");                                                                  scanf("%d", &size);                                                                                     printf("Enter %d numbers:\n", size);                                                                    for (int i=0;i<size;i++){                                                                                       scanf("%lf", &arr[i]);                                                                          }                                                                                                       printf("the average is: %.2f\n", average(arr,size));                                                    printf("the std is: %.2f\n", std_dvitation(arr,size));                                                  printf("the sum is: %.2f\n", sum(arr,size));                                                            return 0;                 
